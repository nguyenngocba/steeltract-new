export async function analyzeFactoryData(
  prompt: string
) {
  return {
    result:
      'AI analyzed operational telemetry successfully.',
    recommendation:
      'Optimize inventory flow in Zone A.',
  }
}
