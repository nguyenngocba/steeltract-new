# VISUAL001 Remaining Opportunities

Date: 2026-07-18

## Pending Browser Validation

Visual composition still needs authenticated screenshots at:

- 1366px
- 1600px
- 1920px
- Ultrawide

## Follow-up Opportunities

- Introduce an explicit `EnterpriseHeroWorkspace` primitive for routes whose
  primary content is a table, queue, board or catalog.
- Add an opt-in `primary` prop to `CockpitChartCard` instead of inferring focal
  treatment from height classes.
- Move older advanced modules onto `EnterpriseWorkspace` where they still use
  bespoke `OperationalShell` layouts.
- Add display-label mapping for backend enum values so table cells are readable
  without changing API contracts.
- Run screenshot comparison against Inventory Golden pages before declaring
  Enterprise Visual Freeze.

